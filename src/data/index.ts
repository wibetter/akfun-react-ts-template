// 引入各级布局元素对象实体
import {
  WidgetContainer,
  WidgetRow,
  WidgetColumn,
  ElemSlot,
  EmbedWidgetSlot,
} from './entity';

/* 系统默认的布局容器数据 */
export const WidgetLayoutObj = {
  ElemSlot,
  EmbedWidgetSlot,
};

/* 常用的布局容器实体 */
export const WidgetLayoutEntity = {
  WidgetContainer,
  WidgetRow,
  WidgetColumn,
  ElemSlot,
  EmbedWidgetSlot,
};
